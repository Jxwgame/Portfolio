# ── dependencies ────────────────────────────────────────────────
node_modules/

# ── next.js / frontend build ────────────────────────────────────
.next/
out/
build/
coverage/
next-env.d.ts
*.tsbuildinfo
.vercel/

# ── go / backend ────────────────────────────────────────────────
backend/data/
backend/server
backend/server.exe

# ── ฐานข้อมูล (ข้อมูลจริง — ห้าม commit) ─────────────────────────
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ── ความลับ: env จริง เก็บไว้เครื่องเดียว ──────────────────────
# .env.example เปิดเผยได้ (มีแต่ชื่อคีย์) จึงยกเว้นไว้
.env
.env.*
!.env.example

# ── ความลับ: กุญแจ / ใบรับรอง / credential ─────────────────────
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks
id_rsa*
*.keystore
credentials.json
service-account*.json
gha-creds-*.json
.npmrc
.netrc

# ── ไฟล์รันเครื่องตัวเอง (log / pid / temp) ─────────────────────
.dev/
*.log
*.pid
*.bak
*.tmp
tmp/

# ── OS / editor ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# ── Claude Code: แชร์ launch.json ได้ ที่เหลือเป็นของเครื่องตัวเอง ─
.claude/*
!.claude/launch.json
